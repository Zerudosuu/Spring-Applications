//Admin dashboard manages users and tickets, allowing admins to view all tickets, assign tickets to users, and manage user accounts. It provides an overview of the system's activity and allows admins to perform administrative tasks efficiently.

function AdminDashboard() {
  // Admin-specific logic can be implemented here, such as fetching all tickets, managing users, etc.

  // 1: Add 2 tabs, where one tab shows all tickets, and the other tab shows all users.
  // 2: In the tickets tab, allow admin to click on a ticket to view details and edit it and maybe assign to the user to correct.
  // 3: In the users tab, show a list of all users with options to edit or delete user accounts.

  return (
    <div>
      <h1>This is Admin dashboard</h1>
    </div>
  );
}

export default AdminDashboard;
